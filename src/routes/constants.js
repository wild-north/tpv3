import { contextify/*, getContextPath*/ } from 'helpers';

const PREFIX = contextify('/');

// export const ORIGIN = window.location.origin;
// export const ROOT_PATH = '/' + getContextPath();

// Initial page
export const INITIAL = PREFIX;